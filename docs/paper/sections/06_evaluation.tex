% 06_evaluation.tex —— 模型评价与推广
\section{模型评价与推广}

\subsection{模型检验}

本文结果不是仅由单一求解器直接给出，而是通过可行解和独立下界共同确认。四个问题的可行调度目标值与下界或验证模型目标值均相等，形成了上下界夹逼关系，如表~\ref{tab:optimality-check} 所示。

\begin{table}[!htbp]
  \centering
  \caption{最优性检验结果}
  \label{tab:optimality-check}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{c r r X c}
    \toprule
    问题 & 可行调度(s) & 下界/验证值(s) & 验证依据 & 是否夹逼最优 \\
    \midrule
    Q1 & 41600 & 41600 & A 车间工序链下界 & 是 \\
    Q2 & 163764 & 163764 & 双瓶颈资源松弛模型 & 是 \\
    Q3 & 123844 & 123844 & C 车间关键链下界 & 是 \\
    Q4 & 123844 & 123844 & 显式采购模型和 C 链下界 & 是 \\
    \bottomrule
  \end{tabularx}
\end{table}

其中 Q2 的下界来自双瓶颈资源松弛模型，证明的是本文双设备同步协同模型下的最优性；Q4 的结果来自显式采购--调度 CP-SAT 模型和 C 车间关键链下界。完整数值结果和 CSV 调度表可由 `baseline\_v1/code/model\_solver.py` 复现，独立下界和采购验证可由 `code/analysis/verify\_optimality\_chain.py` 复现，论文图件可由 `code/plot/plot\_gantt.py` 根据 CSV 结果重新生成。

\subsection{模型优点}

\begin{enumerate}
  \item \textbf{约束覆盖完整。} 模型同时考虑车间内部工序链、双设备协同、同设备互斥、跨车间运输和采购预算等题目核心约束。
  \item \textbf{结果可证明最优。} 四个问题均由可行解和独立下界或显式模型共同支撑，避免了单纯启发式方法无法证明最优的不足。
  \item \textbf{解释性较强。} 问题二可解释为单班组下瓶颈设备限制，问题三和问题四可解释为 C 车间关键链限制，便于从调度管理角度理解结果。
  \item \textbf{复现性好。} 工序时长、运输时间和设备配置均显式结构化，最短时长、CSV 调度表和论文主要图件均可由脚本重新生成。
\end{enumerate}

\subsection{模型局限}

\begin{enumerate}
  \item 双设备工序采用同时起算解释，虽然符合题意，但在更复杂的实际场景中可进一步放宽为异步协同模型。
  \item 当前实例规模较小，精确求解器可以快速证明最优；若车间数、工序数和设备数显著增加，模型求解难度会快速上升。
  \item 采购模型默认新增设备立即可用，且性能与原设备完全一致，未考虑采购周期、维护成本、设备故障等现实因素。
  \item 当前图件主要服务于调度结果展示，后续仍可补充资源负载图、关键路径高亮图和预算敏感性曲线，使图形分析更丰富。
\end{enumerate}

\subsection{推广方向}

后续可从以下方向扩展：第一，将目标函数扩展为工期、采购成本、设备利用率和运输时间的多目标优化；第二，对大规模实例引入遗传算法、禁忌搜索、大邻域搜索等启发式方法，并以本文精确模型作为小规模基准；第三，将设备运输路径从简单两点距离扩展为带道路网络约束的动态路径规划问题；第四，引入不确定工期和设备故障情形，建立鲁棒调度模型。

\subsection{结论}

本文针对多工序协同作业问题建立了统一的多资源调度模型，并对设备分配、工序链、双设备协同、跨车间运输和预算购置等约束进行了系统刻画。通过对附件工序进行展开和结构化处理，原问题被转化为包含 27 个工序实例和 41 个设备子任务的小规模精确调度问题。

求解结果表明，班组 1 完成 A 车间的最短时长为 $41600\,\mathrm{s}$；班组 1 完成五个车间的最短时长为 $163764\,\mathrm{s}$；两个班组共同完成五个车间的最短时长为 $123844\,\mathrm{s}$；在 50 万元设备购置预算下，最短时长仍为 $123844\,\mathrm{s}$，最优采购方案为不购置任何设备。

进一步地，本文分别构造了 A 车间工序链下界、双瓶颈资源松弛下界、C 车间关键链下界和显式采购模型，对四个问题的最优性进行了验证。结果说明，在本文双设备同步协同模型下，问题二的工期主要受单班组瓶颈设备限制；而问题三和问题四的工期由 C 车间不可压缩关键路径决定。该结论不仅满足题目对调度表和购置方案的要求，也为类似多车间协同调度问题提供了精确建模和最优性验证思路。
